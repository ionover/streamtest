# 🎯 Java Streams Training - Начните отсюда!

## ✨ Что готово

Ваша площадка для тренировки Java Streams полностью готова! 

### 📚 12 задач с подсказками:
- **8 задач серии B** (базовые операции)
- **4 задачи серии C** (продвинутые операции)

### 💡 В каждой задаче:
✅ **Комментарий на русском** с описанием что нужно сделать  
✅ **Подсказка в скобках** - какие stream-операции использовать  
✅ **Автоматический тест** - просто запустите и сразу увидите результат  
✅ **Тестовые данные** - уже встроены в каждый класс  

### 📖 Документация:
- **HINTS.md** - подробные подсказки с примерами кода
- **README_TRAINING.md** - полная инструкция
- **EXAMPLE_SOLUTION.md** - примеры готовых решений

---

## 🚀 Быстрый старт (3 шага)

### 1️⃣ Откройте первую задачу
```bash
cd streamtest
cat src/com/orcun/streamtraining/training/TrainingB3.java
```

Вы увидите:
```java
//TODO: Посчитать количество вегетарианцев (используйте filter + count)
public long getCountOfVegetarianUsersV2(List<User> userList) {
    return 0; // Replace with your stream implementation
}
```

### 2️⃣ Реализуйте метод V2
Замените пустую реализацию на streams:
```java
//TODO: Посчитать количество вегетарианцев (используйте filter + count)
public long getCountOfVegetarianUsersV2(List<User> userList) {
    return userList.stream()
        .filter(User::isVegetarian)
        .count();
}
```

### 3️⃣ Запустите тест
```bash
./run.sh TrainingB3
```

Результат:
```
✅ Compilation successful

🧪 Running TrainingB3...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ Test passed!
Result: 5
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**Поздравляю! Первая задача решена!** 🎉

---

## 🏆 ФИНАЛЬНЫЙ ТЕСТ - OHTest

После того как решите основные задачи, проверьте свои знания финальным тестом!

**OHTest** - 10 разнообразных задач без подсказок:
- Работа с числами (суммы, средние, квадраты)
- Работа со строками (поиск, группировка, конкатенация)
- Продвинутые операции (Fibonacci, partitioning, frequency)
- Тест считается пройденным только при **10/10** ✅

```bash
# Запуск финального теста
java -cp bin com.orcun.streamtraining.training.OHTest
```

---

## 📋 Список всех задач с подсказками

### Серия B - Базовые операции

| # | Класс | Задача | Подсказка |
|---|-------|--------|-----------|
| 1 | **TrainingB2** | Найти самого старшего пользователя | `max с Comparator.comparingInt` |
| 2 | **TrainingB3** | Посчитать вегетарианцев | `filter + count` |
| 3 | **TrainingB4** | Получить имена software engineers | `filter + map + collect(toList)` |
| 4 | **TrainingB5** | Первые два вегетарианца | `filter + limit(2) + collect` |
| 5 | **TrainingB6** | Пользователь с зарплатой > X | `filter + findFirst + orElse` |
| 6 | **TrainingB7** | Уникальные имена с зарплатой > X | `filter + map + collect(toSet)` |
| 7 | **TrainingB8** | Фильтр по году + сортировка | `filter + sorted + collect` |
| 8 | **TrainingB9** | Группировка engineers | `filter + groupingBy` |

### Серия C - Продвинутые операции

| # | Класс | Задача | Подсказка |
|---|-------|--------|-----------|
| 9 | **TrainingC1** | Подсчет по должностям | `groupingBy + counting` |
| 10 | **TrainingC2** | Макс зарплата по должностям | `groupingBy + collectingAndThen` |
| 11 | **TrainingC3** | Уникальные языки | `flatMap + distinct + collect` |
| 12 | **TrainingC4** | Знающий английский с макс зарплатой | `filter + max + orElse` |

---

## 🛠️ Полезные команды

```bash
# Запустить одну задачу
./run.sh TrainingB3

# Запустить все задачи и посмотреть прогресс
./run_all.sh

# Скомпилировать все файлы вручную
javac -d bin -sourcepath src $(find src -name "*.java")

# Запустить конкретную задачу вручную
java -cp bin com.orcun.streamtraining.training.TrainingB3
```

---

## 📚 Полезные ресурсы

### 1. **HINTS.md** - Ваша шпаргалка!
Откройте этот файл если застряли. Там есть:
- Подробные объяснения для каждой задачи
- Примеры кода
- Общие паттерны streams
- Частые ошибки

```bash
cat HINTS.md
# или откройте в вашем редакторе
```

### 2. **README_TRAINING.md** - Полная документация
Подробное описание проекта, инструкции, примеры.

### 3. **EXAMPLE_SOLUTION.md** - Готовые решения
Если совсем застряли - посмотрите готовые решения.

---

## 🎯 Рекомендуемый порядок решения

**Начните с простых:**
1. ⭐ TrainingB3 (filter + count)
2. ⭐ TrainingB2 (max)
3. ⭐ TrainingB4 (filter + map)

**Затем средние:**
4. ⭐⭐ TrainingB6 (findFirst)
5. ⭐⭐ TrainingB5 (limit)
6. ⭐⭐ TrainingB7 (toSet)

**Потом сложные:**
7. ⭐⭐⭐ TrainingB8 (sorted)
8. ⭐⭐⭐ TrainingB9 (groupingBy)
9. ⭐⭐⭐ TrainingC1 (groupingBy + counting)
10. ⭐⭐⭐ TrainingC3 (flatMap)
11. ⭐⭐⭐ TrainingC4 (filter + max)

**И самая сложная:**
12. ⭐⭐⭐⭐ TrainingC2 (groupingBy + collectingAndThen)

---

## 💡 Примеры того что вы увидите

### Пример 1: TrainingB3 (простая)
```java
/*
 * Get count of users who are vegetarian
 */
public class TrainingB3 {
    //With for
    public long getCountOfVegetarianUsersV1(List<User> userList) {
        long count = 0;
        for(User user:userList) {
            if(user.isVegetarian()) {
                count++;
            }
        }
        return count;
    }

    //TODO: Посчитать количество вегетарианцев (используйте filter + count)
    public long getCountOfVegetarianUsersV2(List<User> userList) {
        return 0; // <-- ВАШ КОД ЗДЕСЬ
    }
}
```

### Пример 2: TrainingC3 (сложная)
```java
/*
 * Get (distinct) list of languages known by the users
 */
public class TrainingC3 {
    //With for
    public List<Language> getLanguagesKnownV1(List<User> userList) {
        Map<Language,Boolean> languageMap = new HashMap<>();
        for(User user:userList) {
            List<Language> languages = user.getLanguages();
            for(Language language:languages) {
                if(!languageMap.containsKey(language)) {
                    languageMap.put(language, true);
                }
            }
        }
        return new ArrayList<>(languageMap.keySet());
    }

    //TODO: Получить список уникальных языков известных пользователям 
    //      (используйте flatMap для развертывания списков языков + distinct + collect(toList))
    public List<Language> getLanguagesKnownV2(List<User> userList) {
        return new ArrayList<>(); // <-- ВАШ КОД ЗДЕСЬ
    }
}
```

---

## 🎓 Советы для обучения

1. **Читайте комментарий TODO** - там подсказка что использовать
2. **Смотрите на метод V1** - понимайте что он делает
3. **Думайте декларативно** - не "КАК", а "ЧТО" вы хотите получить
4. **Используйте HINTS.md** - там примеры для всех задач
5. **Не спешите** - лучше понять одну задачу хорошо, чем решить все наспех
6. **Запускайте тесты часто** - так вы сразу увидите ошибки

---

## ❓ Если что-то не работает

### Проблема: Не компилируется
```bash
# Убедитесь что Java установлена
java -version
javac -version

# Попробуйте пересобрать
rm -rf bin
mkdir bin
javac -d bin -sourcepath src $(find src -name "*.java")
```

### Проблема: Тест не запускается
```bash
# Проверьте что скрипт исполняемый
chmod +x run.sh run_all.sh

# Запустите вручную
java -cp bin com.orcun.streamtraining.training.TrainingB3
```

---

## 🎉 Удачи в изучении Java Streams!

**Помните:** Streams - это про декларативность. Вместо описания процесса (цикл, условия, счетчики), вы описываете результат который хотите получить.

**Начинайте с TrainingB3** - это самая простая задача! 

```bash
./run.sh TrainingB3
```

И не забывайте заглядывать в **HINTS.md** когда нужна помощь! 💪

---

_Happy Streaming! 🚀_
