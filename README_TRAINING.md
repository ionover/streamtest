# Java Streams Training Playground 🎯

Добро пожаловать на тренировочную площадку для изучения Java Streams!

## 📋 Описание

В этом проекте вы найдете серию задач для практики Java Streams API. Каждая задача имеет:
- ✅ **Метод V1** - решение через обычный цикл `for` (правильный ответ)
- 🔴 **Метод V2** - пустой метод, который вам нужно реализовать через streams
- 🧪 **Main метод** - автоматический тест, который сравнивает результаты V1 и V2

## 🚀 Быстрый старт

### 1. Компиляция проекта
```bash
cd streamtest
mkdir -p bin
javac -d bin -sourcepath src $(find src -name "*.java")
```

### 2. Запуск любой задачи
```bash
# Запустить конкретную задачу
java -cp bin com.orcun.streamtraining.training.TrainingB2
java -cp bin com.orcun.streamtraining.training.TrainingC1
```

### 3. Скрипт для быстрого запуска (опционально)
Создайте файл `run.sh`:
```bash
#!/bin/bash
javac -d bin -sourcepath src $(find src -name "*.java") && \
java -cp bin com.orcun.streamtraining.training.$1
```

Использование:
```bash
chmod +x run.sh
./run.sh TrainingB2
./run.sh TrainingC1
```

## 📚 Список задач

### Серия B (базовые операции)
| Класс | Описание | Сложность |
|-------|----------|-----------|
| **TrainingB2** | Найти самого старшего пользователя | ⭐ |
| **TrainingB3** | Посчитать количество вегетарианцев | ⭐ |
| **TrainingB4** | Получить имена всех software engineers | ⭐ |
| **TrainingB5** | Получить первых двух вегетарианцев | ⭐⭐ |
| **TrainingB6** | Найти любого пользователя с зарплатой > X | ⭐⭐ |
| **TrainingB7** | Получить уникальные имена с зарплатой > X | ⭐⭐ |
| **TrainingB8** | Найти пользователей начавших в год X и отсортировать по зарплате | ⭐⭐⭐ |
| **TrainingB9** | Сгруппировать software engineers по вегетарианству | ⭐⭐⭐ |

### Серия C (продвинутые операции)
| Класс | Описание | Сложность |
|-------|----------|-----------|
| **TrainingC1** | Посчитать количество сотрудников каждой должности | ⭐⭐⭐ |
| **TrainingC2** | Найти максимальную зарплату для каждой должности | ⭐⭐⭐ |
| **TrainingC3** | Получить список уникальных языков | ⭐⭐⭐ |
| **TrainingC4** | Найти пользователя знающего английский с максимальной зарплатой | ⭐⭐⭐ |

## 🎓 Как работать с задачами

### Шаг 1: Выберите задачу
Начните с **TrainingB2** (самая простая)

### Шаг 2: Откройте файл
```bash
# Например
vim src/com/orcun/streamtraining/training/TrainingB2.java
# или используйте ваш любимый редактор
```

### Шаг 3: Изучите метод V1
Посмотрите как задача решена через обычный цикл:
```java
public User getOldestUserV1(List<User> userList) {
    int age = 0;
    User oldestUser = null;
    for (User user : userList) {
        if (age < user.getAge()) {
            age = user.getAge();
            oldestUser = user;
        }
    }
    return oldestUser;
}
```

### Шаг 4: Реализуйте метод V2
Замените пустую реализацию на streams:
```java
public User getOldestUserV2(List<User> userList) {
    // TODO: Ваше решение здесь
    return userList.stream()
        .max(Comparator.comparingInt(User::getAge))
        .orElse(null);
}
```

### Шаг 5: Запустите тест
```bash
javac -d bin -sourcepath src $(find src -name "*.java")
java -cp bin com.orcun.streamtraining.training.TrainingB2
```

### Результат:
- ✅ **Если тест зелёный**: `✓ Test passed!` - Поздравляю, можно переходить к следующей задаче!
- ❌ **Если тест красный**: `✗ Test failed!` - Нужно доработать решение

## 💡 Полезные операции Streams API

### Фильтрация
```java
.filter(user -> user.isVegetarian())
.filter(user -> user.getSalary() > 5000)
```

### Маппинг
```java
.map(User::getName)
.map(user -> user.getLanguages())
```

### Сортировка
```java
.sorted(Comparator.comparing(User::getAge))
.sorted(Comparator.comparing(User::getSalary).reversed())
```

### Ограничение
```java
.limit(2)        // Первые 2 элемента
.skip(3)         // Пропустить первые 3
```

### Поиск
```java
.findFirst()     // Первый элемент
.findAny()       // Любой элемент
.max(...)        // Максимальный
.min(...)        // Минимальный
```

### Агрегация
```java
.count()         // Подсчёт
.collect(Collectors.toList())
.collect(Collectors.toSet())
.collect(Collectors.groupingBy(...))
.collect(Collectors.counting())
```

### FlatMap (для вложенных коллекций)
```java
.flatMap(user -> user.getLanguages().stream())
```

## 🎯 Рекомендуемый порядок решения

1. **Начните с простых**: B2, B3, B4
2. **Переходите к фильтрации**: B5, B6
3. **Освойте distinct и sorting**: B7, B8
4. **Продвинутый уровень**: B9, C1, C2, C3, C4

## 📊 Прогресс
Отмечайте выполненные задачи:
- [ ] TrainingB2
- [ ] TrainingB3
- [ ] TrainingB4
- [ ] TrainingB5
- [ ] TrainingB6
- [ ] TrainingB7
- [ ] TrainingB8
- [ ] TrainingB9
- [ ] TrainingC1
- [ ] TrainingC2
- [ ] TrainingC3
- [ ] TrainingC4

## 🎉 Удачи в изучении Java Streams!

Помните: streams делают код более читаемым и декларативным. Вместо того чтобы описывать **КАК** что-то сделать (императивно), вы описываете **ЧТО** вы хотите получить (декларативно).

---
**Tip**: Если застряли, посмотрите на метод V1 и подумайте:
- Какие операции выполняются? (фильтрация, маппинг, поиск...)
- Как эти операции выразить через streams?
