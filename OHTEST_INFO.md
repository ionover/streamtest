# 🏆 OHTest - Финальный тест навыков Java Streams

## 📖 Описание

**OHTest** - это финальное испытание ваших навыков работы с Java Stream API после прохождения основных задач B2-C4.

В отличие от тренировочных задач, здесь:
- ❌ **НЕТ подробных комментариев с подсказками**
- ❌ **НЕТ отдельных тестов для каждой задачи**
- ✅ **Все 10 задач проверяются одновременно**
- ✅ **Тест считается пройденным только при 10/10**

## 🎯 Список задач

### 1. Сумма квадратов четных чисел
**Задача:** Найти сумму квадратов всех четных чисел в списке

**Пример:**
```java
Input: [1, 2, 3, 4, 5, 6]
Output: 56  // (2² + 4² + 6² = 4 + 16 + 36 = 56)
```

**Подсказка:** `filter + mapToInt + sum`

---

### 2. Самое длинное слово
**Задача:** Найти самое длинное слово в списке

**Пример:**
```java
Input: ["cat", "elephant", "dog", "butterfly"]
Output: "butterfly"
```

**Подсказка:** `max + Comparator.comparingInt + orElse`

---

### 3. Группировка по длине строки
**Задача:** Сгруппировать слова по их длине

**Пример:**
```java
Input: ["hi", "cat", "dog", "bird", "fish"]
Output: {2=["hi"], 3=["cat", "dog"], 4=["bird", "fish"]}
```

**Подсказка:** `collect + Collectors.groupingBy`

---

### 4. Все уникальные символы
**Задача:** Получить множество всех уникальных символов из всех строк

**Пример:**
```java
Input: ["hello", "world"]
Output: [h, e, l, o, w, r, d]
```

**Подсказка:** `flatMapToInt + distinct + collect`

---

### 5. Среднее значение положительных чисел
**Задача:** Найти среднее значение только положительных чисел

**Пример:**
```java
Input: [-5, 2, -3, 4, 6, -1, 8]
Output: 5.0  // (2 + 4 + 6 + 8) / 4 = 5.0
```

**Подсказка:** `filter + mapToInt + average + orElse`

---

### 6. Первые N чисел Фибоначчи
**Задача:** Сгенерировать первые N чисел Фибоначчи

**Пример:**
```java
Input: 8
Output: [0, 1, 1, 2, 3, 5, 8, 13]
```

**Подсказка:** `Stream.iterate + limit + collect`

---

### 7. Разделить числа на четные и нечетные
**Задача:** Разделить список на две группы: четные (true) и нечетные (false)

**Пример:**
```java
Input: [1, 2, 3, 4, 5, 6, 7, 8]
Output: {true=[2, 4, 6, 8], false=[1, 3, 5, 7]}
```

**Подсказка:** `Collectors.partitioningBy`

---

### 8. Конкатенация строк с разделителем
**Задача:** Объединить все строки через запятую в верхнем регистре

**Пример:**
```java
Input: ["java", "streams", "api"]
Output: "JAVA, STREAMS, API"
```

**Подсказка:** `map + Collectors.joining`

---

### 9. Второе максимальное число
**Задача:** Найти второе по величине уникальное число

**Пример:**
```java
Input: [5, 1, 8, 3, 8, 2, 7]
Output: Optional[7]  // Первое макс: 8, второе макс: 7
```

**Подсказка:** `distinct + sorted + skip + findFirst`

---

### 10. Частота появления каждого числа
**Задача:** Подсчитать сколько раз каждое число встречается в списке

**Пример:**
```java
Input: [1, 2, 2, 3, 3, 3, 4, 4, 4, 4]
Output: {1=1, 2=2, 3=3, 4=4}
```

**Подсказка:** `Collectors.groupingBy + Collectors.counting`

---

## 🚀 Как запустить

```bash
# Компиляция (если еще не скомпилировано)
javac -d bin -sourcepath src src/com/orcun/streamtraining/training/OHTest.java

# Запуск теста
java -cp bin com.orcun.streamtraining.training.OHTest
```

---

## 📊 Интерпретация результатов

### ✅ 10/10 - Отлично!
```
🎉🎉🎉 ПОЗДРАВЛЯЕМ! ВЫ МАСТЕР JAVA STREAMS! 🎉🎉🎉
```
Вы полностью освоили Stream API и готовы использовать его в реальных проектах!

### 🟡 7-9/10 - Почти!
Вы на правильном пути! Проверьте задачи которые не прошли и попробуйте снова.

### 🟠 4-6/10 - Нужна практика
Вернитесь к основным задачам (B2-C4) и потренируйтесь еще.

### 🔴 0-3/10 - Начните с основ
Пройдите сначала тренировочные задачи B2-C4 с подсказками.

---

## 💡 Советы

1. **Не смотрите на эталонные решения** сразу - попробуйте сначала сами
2. **Думайте о цепочке операций** - какие промежуточные и терминальные операции нужны
3. **Используйте знания** из задач B2-C4 - там были похожие паттерны
4. **Не спешите** - лучше понять задачу и написать правильно, чем быстро но неверно
5. **Тестируйте по одной задаче** - можно временно закомментировать другие

---

## 🎓 После прохождения

Когда вы пройдете OHTest на 10/10, вы будете готовы:
- ✅ Писать декларативный и читаемый код с streams
- ✅ Использовать продвинутые коллекторы
- ✅ Работать с Optional правильно
- ✅ Применять Stream API в реальных проектах
- ✅ Оптимизировать код заменяя циклы на streams

**Поздравляю с завершением тренинга! 🚀**
